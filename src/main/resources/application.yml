server:
  port: "${port:8110}"

logging:
  level:
    root: INFO
    com.gk.mortgage.calculator: INFO
    
spring: 
  sleuth:
     trace-id128: true

management:
  security:
    enabled: false
    
environment:
  interestrate:
    uri: "http://localhost:8111/interest-rates"